/** @jsx jsx */
import { jsx } from 'theme-ui';

import Layout from '../components/Layout';

const Band = () => (
  <Layout>
    <div sx={{ paddingX: 7, paddingY: 8 }}>
      <p>Band</p>
    </div>
  </Layout>
);

export default Band;